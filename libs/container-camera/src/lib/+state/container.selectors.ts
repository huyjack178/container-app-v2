import { containerFeature } from './container.reducer';

export const {
  selectImages,
  selectLoaded,
  selectDate,
  selectFtpPath,
  selectFtpImages,
  selectFtpImageSrc,
  selectExternalUrls,
  selectContainerId,
} = containerFeature;
