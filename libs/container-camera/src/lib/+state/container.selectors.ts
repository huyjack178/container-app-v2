import { containerFeature } from './container.reducer';

export const {
  selectImages,
  selectLoaded,
  selectDate,
  selectUploadedPath,
  selectUploadedImages,
  selectUploadedImageSrc,
  selectExternalUrls,
  selectContainerId,
} = containerFeature;
